数据类型：
列表是可变的数据类型，下标从0开始，它可以进行增删改查操作，列表可以存储的值的数量和内存大小相关。

元组是不可变的，他会保存每个元素的内存地址，如果元素为一个可变类型如（列表，字典）则可以修改元素内的值。

字符串是按照从左到右的顺序定义的字符集合，下标从0开始，有序，可以进行切片操作
不可变不能像列表一样修改其中的某个元素,所有对字符串的修改都是重新赋值。
字符串转义特殊字符的方法为在赋值操作时前面添加r参数例如：name = r"Jack\n"

字典是 key-value结构，key必须为不可变数据类型，必须唯一
可以存放任意多个value，可修改，可以不唯一
字典是无序的没有下标，查询速度快，不受dict的大小影响。

集合只存储不可变的值如（list，dict数据类型）不可存储在集合里
天生去重，在集合里没办法存重复的元素
无序没有下标.

二进制说明：
二进制只有1和0
二进制前面一位的1代表的10进制数字是后面所有1代表的10进制数字相加的结果
2**8 = 256
1 1 1 1 1 1 1 1 =255
二进制可以用8个1表示255以内的任何10进制数字

hash是把任意长度的输入（又叫做预映射pre-image）通过散列算法变换成固定长度的输出，该输出就是散列值。
hash的特性
不可逆：在具备编码功能的同时，哈希算法也作为一种加密算法存在。即，你无法通过分析哈希值计算出源文件的样子，换句话说：你不可能通过观察香肠的纹理推测出猪原来的样子。
计算极快：20G高清电影和一个5K文本文件复杂度相同，计算量都极小，可以在0.1秒内得出结果。也就是说，不管猪有多肥，骨头多硬，做成香肠都只要眨眨眼的时间，

